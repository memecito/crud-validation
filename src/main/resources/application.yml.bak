spring:
  sql:
    init:
      platform: h2
  application:
    name:
      crud-validation
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:file:./my-database
    driver-class-name: org.h2.Driver
    username: sa
    password: '1234'
  jpa:
    #defer-datasource-initialization: true
    # hibernate:
    #  ddl-auto: update
logging:
  level:
    org.springframework.jdbc.datasource.init: DEBUG